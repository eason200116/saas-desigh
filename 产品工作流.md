# 📌 产品工作流 · 从需求大纲到最终呈现（置顶主线）

> **本文是所有新增/迁移工作的唯一主线，置顶。任何功能必须从阶段 0 进入，不跳阶段、不绕过门控。**
> 它把已有规范串成一条线：需求体系规范（`docs/standards/product-requirements-file-system.md`）+ 原型规范（`docs/standards/prototype-design-spec.md`）+ V3 开发红线（`Ar_V3_LottoPlatformDesign/CLAUDE.md`）。
> 🖼 可视化版：`docs/产品工作流.html`（浏览器打开）。
> 更新：2026-06-15

---

## 一条主线

```
0 需求大纲 ──分流──┬─【新功能】重型 REQ 轨 ─┐
                   └─【迁移页】轻量 MIG 轨 ─┤
                                            ▼
1 需求归档 → 2 原型 → 3 评审 → 4 开发实现 → 5 验收 → 6 最终呈现
   (Draft)   (Appr.    (Appr.    (V3 代码)   (AC+    (发布/
            Prototype) Dev.)                build)   演示)
```

两轨**共用同一条主线**，只是阶段 1 的产出物轻重不同；阶段 2 之后完全一致。

---

## 阶段详解（每阶段：输入 → 动作 → 产出 → 门控）

### 阶段 0 · 需求大纲
- **输入**：业务想法 / `docs/migration-diff-list.md` 待迁移项 / 源盘点（产出至 `docs/migration/`〔规划中〕，源站 = `Ar_V1_Admin` .NET 旧后台）。
- **动作**：写一句话能力定义 + 范围（In/Out scope）+ **分流判断**。
- **分流**：SitINR 没有的真新功能 → **REQ 轨**；SitINR 已有页搬到 V3 → **MIG 轨**（对应 diff-list 的 A/C 类）。
- **门控**：能力定义 + 范围清晰，才进阶段 1。

### 阶段 1 · 需求归档（先归档，再做原型）
- **挖掘前置（DoR · 见个人规则 R17）**：动手前先挖透并确认五项——① 字面要求 ② 隐含意图 ③ 边界/状态（空/加载/错误/无权限/超长/并发）④ 源参照（迁移读源页完整行为，不止列）⑤ Open Questions（标阻塞级）。挖不透不进归档。
- **REQ 轨**：建 `docs/requirements/REQ-xxx-name/` **多文件包**（按规范 §4：`00-brief` → `01-capability-spec` → `02-business-rules` + `03-flows` → `04-screens` → `05-data-permission` → `06-acceptance` → `99-change-log`）。模板见 `docs/templates/requirement-package-template.md`。
- **MIG 轨**：用 `docs/templates/migration-page-template.md` 填**单页对照**（源页盘点 → 目标页专属 → 差异对照 → 字段对齐自检 → 验收）。通用交互不重写，引用 `docs/saas-phase2-feature-spec.html §二`。
- **纪律**（agent.md §3）：已拍板规则 / `OPEN-xxx` 未决问题 / 临时假设 三类分清，不混写；不把规则藏在原型里。
- **状态**：`Draft → Reviewing`。
- **门控**：达到 **Approved for Prototype** 最低要求（目标/规则/主流程/页面范围明确，未决问题不影响原型）。

### 阶段 2 · 原型
- **动作**：建 `prototypes/REQ-xxx-name.html`，遵循**原型规范**（视觉 token / 控件 / 交互 / 层级 / 验收清单）。
- MIG 轨多数复用现有页与通用交互规范，**可不单独建原型**。
- **状态**：`Approved for Prototype`。
- **门控**：原型可表达核心交互。

### 阶段 3 · 评审
- **动作**：按规范 §8 顺序评审 6 问（目标→范围→规则冲突→数据权限→交互服务规则→验收可测）；跑 `06-acceptance` 的开发前 Ready 检查。
- **门控**：**所有 `Blocks Development` 的 open questions 关闭**，不留"开发时再定"的核心规则 → `Approved for Development`。

### 阶段 4 · 开发实现（V3：`Ar_V3_LottoPlatformDesign`）
- **新功能**：走 superpowers 链路（brainstorming → planning → TDD → code-review，见 CLAUDE.md §15.1）。
- **迁移页**：走 `list-page` skill；新增列/功能用 `src/utils/newMark.ts` 标「新」；字段对齐按 CLAUDE.md §5.3 自检。
- **红线**：100% 本地 mock、无真实 HTTP、`pnpm build` 必过。
- **产出**：代码 + 把改动誊写进 `Ar_V3_LottoPlatformDesign/docs/migration-changes.md`。

### 阶段 5 · 验收（DoD 门控 · 见个人规则 R18）
- **动作**：逐条点验 `06-acceptance` / MIG 的 AC；跑 `pnpm typecheck && lint && lint:i18n && build`。
- **DoD 完成定义（全过才算 Accepted，不准"差不多"当"做完"）**：
  1. 功能 AC 全过；
  2. **真验证**：build 绿 + i18n 运行无原文键 + 跑通关键点击路径 + 边界态（空/加载/错误/无权限/超长）都看过——**不只 build**；
  3. **无半成品**：路由/组件成对、占位必标记、剩余 backlog 显式列清；
  4. 自查错误归档。
- **门控**：DoD 全过 → `Accepted`；只完成一部分必须明说 `X/Y 完成 + 剩余清单`，不藏。

### 阶段 6 · 最终呈现
- **原型**：发布 GitHub Pages（`eason200116/saas-desigh` 标准流程）。
- **V3**：演示页面 / 截图交付。
- **收尾**：`99-change-log` / `migration-changes.md` 记录本次变更与决策人。

---

## 状态总览（贯穿全程）

`Draft → Reviewing → Approved for Prototype → Approved for Development → Accepted → Published`
（异常：`Blocked` / 废弃：`Deprecated`）

## 两轨对照速查

| | 重型 REQ 轨 | 轻量 MIG 轨 |
|---|---|---|
| 用于 | 真新增功能（diff-list B 类 24） | 迁移已有页（diff-list A 72 + C 59） |
| 阶段 1 产出 | 多文件 REQ 包（按标准） | 单文件 MIG 对照（v0.1，过程中迭代） |
| 业务规则/流程 | 从零定义 | 已知，不重写 |
| 原型 | 必做 | 多数复用，可省 |
| 开发 | superpowers 链路 | list-page skill |

---

## 置顶纪律
1. 任何功能从**阶段 0** 进入，不跳阶段。
2. 每个**门控**不达标，不进下一阶段。
3. 同一概念只在一处定义，其他地方引用（规范 §9）。
4. 未拍板的写 `OPEN-xxx`，不写成确定语气。
