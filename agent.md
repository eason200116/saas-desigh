# Agent 基本原则

适用范围：本仓库内所有产品梳理、需求文档、原型设计和后续开发协作。

## 1. 先读上下文

开始任何工作前，先阅读：

- `README.md`
- `docs/handoff.md`
- `docs/standards/product-requirements-file-system.md`
- 当前任务关联的 `docs/product/` 或 `docs/requirements/REQ-xxx-*`

不要只根据最近一段对话改文件。已有文档里的已拍板原则优先级高于临时推测。

## 2. 需求先归档，再做原型

新增需求必须先建立或更新需求包：

```text
docs/requirements/REQ-xxx-short-name/
```

需求包至少要明确：

- 背景、目标、范围、非目标。
- 能力定义。
- 业务规则。
- 主流程、分支流程、失败流程。
- 页面和交互范围。
- 数据归属、权限、审计。
- 验收标准。

原型只表达交互，不作为唯一规则来源。不能把业务规则藏在 HTML 原型里。

## 3. 区分三类内容

必须分清：

- 已拍板规则：写入业务规则或 decision log。
- 待确认问题：写入 open questions，并标注阻塞级别。
- 临时假设：只能作为假设出现，不能写成确定结论。

不要把讨论记录、结论、问题和原型说明混在同一个文档里。

## 4. 维护主骨架

当前阶段先理清 SaaS 包网主骨架，不随意扩展完整经营功能。

主骨架包括：

- 平台
- 商户
- 品牌
- 市场模板
- 域名
- 渠道
- 玩家
- 代理关系
- 商户团队权限
- 四端入口
- 配置继承

游戏厂商、支付账务、注单结算、活动优惠、返水佣金、客服工单、KYC / AML、消息通知、CMS、平台计费和运维监控后续可以补，但必须挂在主骨架下，不另起业务层级。

## 5. 遵守已拍板产品原则

这些原则不能随意改：

- 商户是合同、计费、团队和多品牌经营的归属边界。
- 品牌是玩家、钱包、订单、活动、风控、渠道和报表的核心隔离边界。
- 玩家单位数据必须按品牌严格隔离。
- 一个品牌整个生命周期只经营一个币种。
- 多币种通过多个品牌承载，可在品牌上设计同一系列。
- 不同币种金额不能直接混加，不做汇率折算。
- 非金额指标也要按配置规则分开，不默认跨市场混合。
- 市场模板唯一键以国家为单位；币种和语言是市场模板属性。
- 商户模板由多个市场模板组成，不保留独立通用模板。
- 平台默认模板不自动覆盖已启用的商户市场模板。
- 品牌必须基于某一个市场模板创建，并默认继承该市场模板。
- 域名从属于品牌，可被同品牌渠道绑定。
- 渠道只能绑定同品牌下可用域名，不能跨品牌使用其它品牌域名。
- 来源归因、代理关系、业绩归集三者分离。
- 不提供转代功能。

如需调整以上原则，必须先写明原因、影响范围和替代规则，再等待确认。

## 6. 配置体系原则

配置中心只保留两层数据模型：

- 市场模板。
- 品牌覆盖。

不增加“品牌组模板”或“同市场品牌组继承层”。

前端操作范围：

- 市场模板配置。
- 同市场品牌组配置。
- 单一品牌配置。

同市场品牌组配置只是批量写入品牌覆盖，不是长期继承层。

多品牌多市场时，市场模板必须先选择单一国家市场后再编辑；跨市场只允许展示和切换，不提供同时写入多个市场模板的操作。

## 7. 前端和原型原则

平台端、商户端只做电脑版管理工作台。

设计顺序：

1. 先覆盖多品牌多市场。
2. 再收敛到多品牌单一市场。
3. 最后收敛到单一品牌单一市场。

单一品牌和多品牌必须复用同一套前端，不为单一品牌另做一套页面。

原型要求：

- 先看到原配置，再做变更。
- 提交前展示作用对象、原值、提交后值、来源层级和差异摘要。
- 复杂配置不能只做“选择方案”，必须能编辑方案细节。
- 数组类配置要支持新增、修改匹配项、移除匹配项，避免误替换整套规则。
- 单品牌状态是锁定态，不是另一套产品。

## 8. 文件组织原则

目录职责：

- `docs/product/`：项目级长期产品上下文。
- `docs/requirements/`：按编号管理独立需求包。
- `docs/standards/`：文档体系、命名和评审规范。
- `docs/templates/`：可复制模板。
- `prototypes/`：可打开审阅的 HTML 原型。

新增文件必须放到正确目录。不要在根目录堆临时文档。

新增原型推荐命名：

```text
prototypes/REQ-xxx-short-name.html
```

截图、临时导出、实验文件默认不入库，除非用户明确要求保留。

## 9. Git 原则

每次修改前先看：

```text
git status --short
```

规则：

- 不覆盖用户未提交修改。
- 不使用破坏性 git 命令。
- 有意义的文档或原型变更完成后要提交。
- commit 信息使用简洁英文，例如 `docs: add requirement standard`。
- 提交前检查 diff，确认没有无关文件。

## 10. 验证原则

文档变更至少检查：

- 路径引用是否正确。
- Markdown 是否清晰可读。
- 是否把已拍板规则和 open questions 混写。

HTML 原型变更至少检查：

- 内联脚本可解析。
- 页面能打开。
- 关键交互能操作。
- 文案没有与已拍板规则冲突。

如果无法验证，必须在回复中说明原因。

## 11. 协作原则

默认用中文沟通。

回复要说明：

- 改了什么。
- 为什么这么改。
- 验证了什么。
- git 提交号。

当需求含糊但风险不高时，按现有规范做合理判断并继续推进；当可能改变核心业务规则时，先指出冲突和影响，再等用户确认。
